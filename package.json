{
  "name": "cslockin",
  "version": "0.1.0",
  "description": "LockIn: a grind tracker for CS students on the job hunt. Hosted at cslockin.com.",
  "private": true,
  "type": "module",
  "license": "MIT",
  "repository": { "type": "git", "url": "https://github.com/ma9197/LockIn" },
  "scripts": {
    "dev": "wrangler dev --port 8787",
    "deploy": "wrangler deploy",
    "check": "node scripts/check.mjs",
    "db:migrate:local": "wrangler d1 migrations apply cslockin-central --local",
    "db:migrate:remote": "wrangler d1 migrations apply cslockin-central --remote"
  },
  "dependencies": {
    "hono": "^4.13.4"
  },
  "devDependencies": {
    "wrangler": "^4.126.0"
  }
}
